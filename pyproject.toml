[project]
name = "mood-tracker"
version = "0.1.0"
description = "Mood tracker PWA with push notifications"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "pywebpush>=2.0",
    "cryptography>=44",
]
